<%= form_for @shopping_list, :html => { :class => "form-horizontal shopping_list" } do |f| %>

  <% if @shopping_list.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@shopping_list.errors.count, "error") %> prohibited this shopping_list from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @shopping_list.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%=f.error_span(:name) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                shopping_lists_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
